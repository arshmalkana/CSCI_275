# Override the backend API base URL in production.
# In dev, the Vite proxy rewrites /v1/* to http://localhost:8080, so leave this unset.
# In prod, set to your backend URL (without trailing slash):
VITE_API_BASE_URL=https://api-ahpunjab.itsarsh.gov.in/v1
