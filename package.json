{
  "name": "ahpunjab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run db && concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd Backend && npm run dev",
    "dev:frontend": "cd PWA && npm run dev",
    "db": "docker compose up -d postgres pgadmin",
    "db:down": "docker compose down",
    "install:all": "cd Backend && npm install && cd ../PWA && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
